object Main 
{
    // Main Method 
    def main(args: Array[String]) 
    {
        val a: Int = 25
        val b: Int = -25 
        val c: Int = 3 
        val d: Int = -3
        println(a + " % " + c + " = " + (a%c))
        println(a + " % " + d + " = " + (a%d))
        println(b + " % " + c + " = " + (b%c))
        println(b + " % " + d + " = " + (b%d))
    }
}